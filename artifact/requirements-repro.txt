# Reproduction-only dependencies (no GPU, no mitmproxy, no Playwright).
# Sufficient to regenerate every table and figure in the paper from the
# Tier-1 aggregates shipped under each rq*_/data/ directory.
#
# Pinned to the versions used to produce the camera-ready numbers.

numpy==2.4.2
scipy==1.17.0
matplotlib==3.9.2
pandas==2.2.3
